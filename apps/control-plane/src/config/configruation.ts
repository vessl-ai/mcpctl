import { appConfiguration } from './app.config';
import { cacheConfiguration } from './cache.config';
import { secretConfiguration } from './secret.config';

export const configurations = [
  appConfiguration,
  cacheConfiguration,
  secretConfiguration,
];
